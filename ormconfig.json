{
  "type": "postgres",
  "host": "postgres",
  "port": 5432,
  "username": "postgres",
  "password": "secret",
  "database": "trello",
  "entities": ["build/**/*.entity{.ts,.js}"],
  "migrations": ["build/migrations/*{.ts,.js}"],
  "migrationsRun": true,
  "cli": {
    "migrationsDir": "src/migrations"
  }
}
